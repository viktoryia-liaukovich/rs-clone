const variables = {
  currentLevel: 0,
  timerId: 0,
  childMode: false,
  isGameInProgress: true,
  music: false,
  sounds: false,
  fullscreen: Boolean(document.fullscreenElement),
};

export default variables;
